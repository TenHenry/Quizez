name: quizez
description: "Quizez - App to learn in smart way"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.4
  isar: ^3.1.0
  isar_flutter_libs: ^3.1.0
  path_provider: ^2.1.5

  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  get_it: ^9.2.1

  http: ^1.6.0
  web_socket_channel: ^3.0.3

  fl_chart: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.8
  isar_generator: ^3.1.0

flutter:

  uses-material-design: true
